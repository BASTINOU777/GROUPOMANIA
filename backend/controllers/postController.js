//importation du package File system de Node
const fs = require("fs");
//importation du model post
const Post = require("../models/post-model");

// Création d'un post via un user
//exports.createPost = (req, res, next) => 